Call for chain X plus chain Y plus chain Z.

To model:
chain1(X) : histone-linker-dom1_A-linker-histone
chain2(Y) : dom1_B-linker-dom2_A
chain3(Z) : histone-linker-dom2_B-linker-histone
bound domains: dom1_A-dom1_B, dom2_A-dom2_B

Use the pdb obtained in the previous step. The entire sequence of chain X and Y
must be inside of dom2_B, both in the pdb and in the sequence file

Call to ranch:
ranch seqXYZ.fasta -q=10 -x=histone.pdb -x=00002chainXY_mod_fixed_mod.pdb -x=histone.pdb -f=no -f=no -f=no -o=no -o=no -o=no --filesuffix=chainXYZ -w=chainXYZ -i

After the run, the resulting pdbs have to be modified to move the sequence
of the chain X and Y at the beginning of the pdb (or the end, before any HETATMs)
as a separate chain

# NOTE: move chains to the end and set corresponding name
